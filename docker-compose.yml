version: '3.3'
services:
  evolution-api:
    image: atendai/evolution-api:latest
    container_name: evolution_api_ceia
    restart: always
    ports:
      - "8081:8080"
    environment:
      - SERVER_URL=http://localhost:8081
      - AUTHENTICATION_API_KEY=CEIA_CHAVE_MESTRA_2026
      - OS_TIMEOUT=30000
    command: ["node", "./dist/src/main.js"]
